describe("A suite2", () => {
  it("1contains spec with an expectation", () => {
    expect(true).toBe(true);
  });
  it("1contains spec with an expectation 2", () => {
    expect(true).toBe(false);
  });
});
